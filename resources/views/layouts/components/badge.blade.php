    <div class="badge badge-{{ $type or 'pill'}} badge-{{ $color or 'primary'}} d-inline-flex align-items-center">{{ $filter }}
        <button type="button" class="close pl-1" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>